apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: changetrace
  labels:
    app: inventory-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      imagePullSecrets:
        - name: acr-pull-secret
      containers:
        - name: inventory-service
          image: changetrace29r2ywacr.azurecr.io/demo_app:latest
          ports:
            - containerPort: 8080
          env:
            - name: SERVICE_NAME
              value: "inventory-service"
            - name: DEPENDENCIES
              value: '[]'
            - name: PORT
              value: "8080"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.changetrace.svc.cluster.local:4318"
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: LOG_LEVEL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: changetrace
  labels:
    app: inventory-service
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: inventory-service
